/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["https://lh3.googleusercontent.com"], //make it 'your-domain.com'
  },
};

module.exports = nextConfig;
